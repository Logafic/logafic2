import 'package:flutter/material.dart';

class UserInformationDropdownCity extends StatelessWidget {
  const UserInformationDropdownCity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
